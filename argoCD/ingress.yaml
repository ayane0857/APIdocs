---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  labels:
    name: app-ingress
spec:
  ingressClassName: traefik
  rules:
    - host: api.ayane0857.net
      http:
        paths:
          - pathType: Prefix
            path: "/docs"
            backend:
              service:
                name: ayane-api-document-site
                port:
                  number: 80
